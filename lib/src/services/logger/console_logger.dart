import 'package:firebase_remote_config_generator/src/services/logger/logger.dart';

class ConsoleLogger implements Logger {
  @override
  void log(String message) {
    throw UnimplementedError();
  }
}
