name: firebase_remote_config_generator
description: A new Flutter package project.
version: 0.0.1
homepage: https://github.com/mikeborodin/firebase_remote_config_generator

environment:
  sdk: ">=2.17.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

executables:
  frc_generator: firebase_remote_config_generator
